"use strict" // treat all js code as new versions
let name = "mayur"
let age=23
let islogged="false"
let state;
// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique
// object
// Storing or printing text => "Hello" or 'Mayur'
// Comparing with string values => if (name === "Mayur")
// Returning string from a function => return "Done"


console.log(typeof undefined); // undefined
console.log(typeof null); // object
console.log({ age :typeof age, name : typeof name, islogged : typeof islogged })
